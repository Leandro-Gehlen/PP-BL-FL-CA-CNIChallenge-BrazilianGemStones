export type User = {
    id: string
    firstName: string
    lastName: string
    email: string
    password: string

}

export type Stone = {
    id: string
    name: string
    imgStoneURL: string
    imgStoneLapidationURL: string
    description: string
}
export type User = {
    firstName: string
    lastName: string
    email: string
    password: string
}

export type Stone = {
    name: string
    imgStoneURL: string
    imgStoneLapidationURL: string
    description: string
}